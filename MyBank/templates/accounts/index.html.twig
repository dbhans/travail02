{% extends 'base.html.twig' %}

{% block title %}Accounts index{% endblock %}

{% block body %}
    <h1>Accounts index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Account</th>                
                <th>Type</th>
                <th>Customer ID</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for account in accounts %}
        
            <tr>
                <td>{{ account.id }}</td>
                <td>{{ account.account }}</td>                
                <td>{{ account.type }}</td>
                <td>{{ account.customer }}</td>
                <td><a href="{{ path('accounts_show', {'id': account.id}) }}">show</a></td>
                <td>
                    <a href="{{ path('accounts_show', {'id': account.id}) }}">show</a>
                    <a href="{{ path('accounts_edit', {'id': account.id}) }}">edit</a>
                </td>
            </tr>
        
        
            
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('customer_accounts', {'customer':customer}) }}">Create new</a>
{% endblock %}
